{
  "checks": [
    {"name": "compiles", "type": "cmd",
     "cmd": ["${PYTHON}", "-m", "compileall", "-q", "."],
     "timeout": 180}
  ],
  "live": [
    {"name": "compiles", "type": "cmd",
     "cmd": ["${PYTHON}", "-m", "compileall", "-q", "."],
     "timeout": 180}
  ]
}
